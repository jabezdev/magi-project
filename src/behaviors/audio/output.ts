import { LibraryItem } from '../../types'

export function renderAudioOutput(_item: LibraryItem, _container: HTMLElement) {
    // Audio is typically background or routed, not projected.
    // We can render nothing or debug info.
}
