# Video Placeholder

Place your background video files here. Supported formats:
- MP4 (.mp4)
- WebM (.webm)
- OGG (.ogv)

Example: `background.mp4` - This will be used as the default looping background video for the Main Projection Screen.

The video path should be referenced in `/data/lyrics.json` under `defaultBackgroundVideo`.
